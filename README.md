# 🐬 MySQL Queries

Este repositorio contiene un conjunto de **consultas SQL** resueltas sobre la base de datos de ejemplo **Sakila**, enfocadas en practicar y dominar diversas sentencias y operaciones en SQL.

---

## 📂 Contenido del repositorio

- `MySQL Queries.sql`: Archivo con la serie completa de consultas y ejercicios SQL resueltos.
- `README.md`: Descripción del proyecto y guía rápida.

---

## 🎯 Objetivo del ejercicio

El propósito es aplicar conocimientos de SQL para extraer, filtrar, agrupar y manipular datos mediante consultas que resuelven diferentes problemas sobre una base de datos realista (Sakila).

---

## 🧩 Ejercicios principales realizados

Las consultas cubren una amplia variedad de temas, tales como:

### 🔍 Consultas básicas y filtros

- Selección de columnas específicas sin duplicados (`DISTINCT`).
- Filtrado por condiciones (`WHERE`, `LIKE`, `IN`, `BETWEEN`).
- Búsqueda de texto en descripciones (`LIKE`, expresiones regulares).

### 🧮 Funciones agregadas y agrupamientos

- Conteo de registros por categorías (`COUNT`, `GROUP BY`, `HAVING`).
- Cálculo de promedios (`AVG`).
- Ordenamientos (`ORDER BY`).

### 🔗 Joins y subconsultas

- Relaciones entre tablas con `INNER JOIN`, `LEFT JOIN`.
- Uso de subconsultas para filtros avanzados (`EXISTS`, subqueries en `WHERE`).
- Unión de resultados con `UNION` y `UNION ALL`.

### 🕵️ Consultas avanzadas

- Identificación de actores sin películas asignadas.
- Agrupación y concatenación de categorías por actor (`GROUP_CONCAT`).
- Cálculo de duración de alquileres y filtrado por tiempo.
- Selección de películas según múltiples criterios combinados.

---

## 🛠️ Tecnologías y conceptos aplicados

- MySQL / MariaDB
- Manipulación avanzada de datos con SQL
- Optimización y legibilidad de consultas
- Uso efectivo de índices y claves foráneas (implícito en diseño Sakila)

---

## 🚀 Cómo usarlo

1. Descarga el archivo `.sql` del repositorio.
2. Carga el archivo en tu entorno MySQL con la base de datos **Sakila** instalada.
3. Ejecuta las consultas una por una para analizar resultados y entender su funcionamiento.
4. Modifica o expande las consultas para practicar y adaptarlas a nuevos casos.

---

## 🙌 Agradecimientos

Este conjunto de ejercicios es ideal para estudiantes y profesionales que buscan afianzar sus habilidades en SQL, tanto para evaluaciones académicas como entrevistas técnicas.

---

¡Explora, aprende y domina SQL con estos ejercicios prácticos! 💻📊🐬
